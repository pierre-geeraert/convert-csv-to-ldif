class ldap:
    id_ldap = "cn=admin,dc=server,dc=com"
    password_ldap = "password"
    server_ldap = "server.comeu"
    baseDN_ldap = "dc=server,dc=com"

class pushbullet:
    token_pushbullet = "token_push"

class vcf_file:
    path = "Path of the vcf file"
